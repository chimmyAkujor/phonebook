import React, { Component } from 'react';
import './Profile';

class Header extends Component{
    render(){
        return(
            <header>
                <h1>Contacts</h1>
            </header>
        );
    }
}

export default Header;